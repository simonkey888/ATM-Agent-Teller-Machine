name: ATM Cloud Cycle Scheduled

on:
  workflow_dispatch:

permissions:
  contents: read
  issues: read

jobs:
  legacy-scheduler-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ATM_LEGACY_GITHUB_CRON=DISABLED"
          echo "AUTHORITY=CLOUDFLARE_CRON_V1"
